spring:
  application:
    name: salida-service
  cloud:
    config:
      uri: http://localhost:8081/
  datasource:
    url: jdbc:postgresql://localhost:5432/salida
    username: postgres
    password: 112233
  jpa:
    hibernate:
      ddl-auto: update